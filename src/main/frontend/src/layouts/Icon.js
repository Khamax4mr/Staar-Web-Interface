import {Help, Search} from '@mui/icons-material';
import styled from '@emotion/styled';

const HelpIcon = styled(Help)({
  color: 'black',
});

const SearchIcon = styled(Search)({
  color: 'black',
});

export {HelpIcon, SearchIcon};