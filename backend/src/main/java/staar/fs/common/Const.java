package staar.fs.common;

/* 웹 UI 스프링 백엔드 파일 탐색 구현에 사용한 상수 클래스입니다. */
public class Const {

  /* 파일 경로. */
  public static final class PATH {
    public static final String BENCHMARK = "/home/shared";
  }
}
